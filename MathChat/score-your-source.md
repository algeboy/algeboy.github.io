---
layout: page
width: expand
title: Try your own source
permalink: /MathChat/score-your-source/
description: Score a source provisionally against the MathChat viewpoint map.
---

<p><a href="/MathChat/">← Viewpoint map</a></p>

<p>This is a provisional, browser-only comparison against the sources on MathChat’s map. It is not an assessment of a person, institution, or author. To submit an arXiv paper or captioned YouTube video, enter only your name and its link: source details and the score input are loaded automatically.</p>

<div class="mathchat-tool-card mathchat-tool-warning"><strong>Your file stays on your device.</strong> Its text is read only in this browser; nothing is uploaded by this page. The automatic scores use visible language cues and source-type information, so please review them before using or sharing them.</div>

<div class="mathchat-tool-card">
  <label for="file">Source file (plain text, Markdown, CSV, or HTML)</label>
  <input id="file" type="file" accept=".txt,.md,.markdown,.csv,.html,.htm,text/plain,text/markdown,text/csv,text/html">
  <small>For PDFs and Word documents, paste the text below instead.</small>
  <label for="title">Title or source name</label>
  <input id="title" type="text" placeholder="e.g., My classroom AI policy">
  <label for="submitter-name">Your name <span aria-hidden="true">*</span></label>
  <input id="submitter-name" type="text" required autocomplete="name" placeholder="Name for the review record">
  <small>Your name is required for a review submission and will be included in the prepared GitHub issue.</small>
  <label for="arxiv-link">arXiv link (optional)</label>
  <input id="arxiv-link" type="url" inputmode="url" placeholder="https://arxiv.org/abs/2608.16753">
  <small>We score the complete public arXiv HTML paper, not its abstract. The abstract is stored only as a position summary.</small>
  <small>You never need to upload the paper file.</small>
  <p id="arxiv-status" aria-live="polite"></p>
  <label for="youtube-link">YouTube link (optional)</label>
  <input id="youtube-link" type="url" inputmode="url" placeholder="https://www.youtube.com/watch?v=…">
  <small>We score the public English transcript, not the video title or description. Use either an arXiv link or a YouTube link, not both.</small>
  <p id="youtube-status" aria-live="polite"></p>
  <label for="text">Source text <small>(only needed when not using arXiv)</small></label>
  <textarea id="text" placeholder="Paste source text here, or choose a file above."></textarea>
  <button id="score" type="button">Score this source</button>
</div>

<section id="result" class="mathchat-hidden" aria-live="polite">
  <h2>Provisional comparison</h2>
  <div class="mathchat-scores">
    <div class="mathchat-score"><b id="outlook">–</b>Outlook<br><small>anxious 0 → hopeful 100</small></div>
    <div class="mathchat-score"><b id="evidence">–</b>Evidence basis<br><small>speculative 0 → data-supported 100</small></div>
    <div class="mathchat-score"><b id="reliability">–</b>Source reliability<br><small>lower 0 → higher 100</small></div>
  </div>
  <p id="comparison"></p><p id="explanation"></p>
  <div class="mathchat-tool-card">
    <h2>Optional appendix submission</h2>
    <p>Submissions are <strong>not automatically published</strong>; they require review before they can join the public appendix. By preparing one, you confirm it is tidy, polite, and apolitical.</p>
    <button id="prepare" type="button" disabled>Prepare appendix submission</button><p id="submission-note"></p>
  </div>
</section>

<style>
.mathchat-tool-card { border:2px dashed var(--chalk-white); border-radius:.6rem; padding:1rem; margin:1rem 0; background:rgba(26,74,58,.38); }
.mathchat-tool-warning { border-left: .3rem solid var(--chalk-yellow); } .mathchat-tool-card label { display:block; font-weight:650; margin:.8rem 0 .3rem; }
.mathchat-tool-card input, .mathchat-tool-card textarea, .mathchat-tool-card button { font:inherit; } .mathchat-tool-card input[type=text], .mathchat-tool-card input[type=url], .mathchat-tool-card textarea { width:100%; box-sizing:border-box; padding:.55rem; color:var(--chalk-white); background:rgba(0,0,0,.22); border:1px solid var(--chalk-dust); border-radius:.3rem; }
.mathchat-tool-card textarea { min-height:8rem; } .mathchat-tool-card button { padding:.55rem .8rem; cursor:pointer; color:var(--chalk-white); background:transparent; border:2px solid var(--chalk-white); border-radius:.3rem; } .mathchat-tool-card button:hover { color:var(--chalk-yellow); border-color:var(--chalk-yellow); } .mathchat-tool-card button:disabled { cursor:not-allowed; opacity:.5; }
.mathchat-scores { display:grid; grid-template-columns:repeat(3,1fr); gap:.75rem; } .mathchat-score { padding:.7rem; border-radius:.45rem; background:rgba(248,248,240,.09); border:1px solid rgba(248,248,240,.45); } .mathchat-score b { display:block; font-size:1.65rem; color:var(--chalk-yellow); } .mathchat-hidden { display:none; }
@media (max-width:700px) { .mathchat-scores { grid-template-columns:1fr; } }
</style>

<script>
const references=[['Su',82,35,60],['Bessis',54,38,68],['Economist',28,86,72],['Yahoo',30,70,55],['Williamson',78,62,80],['Tao',61,78,88],['Weinreich',12,48,65],['Leiden',24,72,82],['AI Snake Oil',38,78,82],['Gowers',58,82,90],['Avigad',64,76,88],['Conrad Wolfram',72,68,78],['Stephen Wolfram',78,55,74],['Tsimerman',88,55,76],['LeCun',65,65,84],['Riehl',56,86,90],['Lanier',60,36,64],['Marcus',32,65,84],['Cepelewicz',64,76,82],['Strogatz',60,70,84],['Carroll',55,54,76],['Jaimungal',76,55,70],['Hossenfelder',58,45,64],['Keating',61,58,74]],$=id=>document.getElementById(id);let scored,arxivSnapshot=null,arxivPaperText='',youtubeSnapshot=null,youtubeTranscript='';
$('file').addEventListener('change',async e=>{const f=e.target.files[0];if(!f)return;$('text').value=await f.text();if(!$('title').value)$('title').value=f.name.replace(/\.[^.]+$/,'')});
const arxivId=value=>{const m=value.trim().match(/(?:arxiv\.org\/(?:abs|pdf|html)\/|export\.arxiv\.org\/api\/query\?id_list=)([a-z-]+(?:\.[A-Z]{2})?\/\d{7}|\d{4}\.\d{4,5})(?:v\d+)?/i);return m?m[1]:''},atomText=(node,name)=>node.querySelector(name)?.textContent.trim()||'';
async function loadArxiv(){const id=arxivId($('arxiv-link').value);arxivSnapshot=null;arxivPaperText='';if(!$('arxiv-link').value.trim()){$('arxiv-status').textContent='';return}if(!id){$('arxiv-status').textContent='Enter a valid arXiv link.';return}$('arxiv-status').textContent='Reading the complete public arXiv paper…';try{const metadata=await fetch(`https://export.arxiv.org/api/query?id_list=${encodeURIComponent(id)}`);if(!metadata.ok)throw new Error(`metadata returned ${metadata.status}`);const xml=new DOMParser().parseFromString(await metadata.text(),'application/xml'),entry=xml.querySelector('entry');if(!entry||xml.querySelector('parsererror'))throw new Error('metadata was unreadable');const authors=[...entry.querySelectorAll('author')].map(author=>{const name=atomText(author,'name'),affiliation=atomText(author,'arxiv\\:affiliation');return{name,affiliation,profileUrl:`https://arxiv.org/search/?query=${encodeURIComponent(name)}&searchtype=author`}}),summary=atomText(entry,'summary').replace(/\s+/g,' ');arxivSnapshot={id,url:`https://arxiv.org/abs/${id}`,title:atomText(entry,'title').replace(/\s+/g,' '),summary,primaryCategory:entry.querySelector('arxiv\\:primary_category, category')?.getAttribute('term')||'',authors};if(!$('title').value.trim())$('title').value=arxivSnapshot.title;const paperResponse=await fetch(`https://arxiv.org/html/${encodeURIComponent(id)}`);if(!paperResponse.ok)throw new Error(`full HTML returned ${paperResponse.status}`);const paper=new DOMParser().parseFromString(await paperResponse.text(),'text/html'),paperRoot=paper.querySelector('main, article, #content')||paper.body;arxivPaperText=(paperRoot?.textContent||'').replace(/\s+/g,' ').trim();if(arxivPaperText.length<1000)throw new Error('full HTML did not contain enough paper text');arxivSnapshot.paperTextLength=arxivPaperText.length;$('arxiv-status').textContent=`Read ${arxivPaperText.length.toLocaleString()} characters from the full paper. The score will not use the abstract.`}catch(error){$('arxiv-status').textContent=`Could not read the complete arXiv paper: ${error.message}. This linked source cannot be scored from an abstract.`}}
$('arxiv-link').addEventListener('change',loadArxiv);$('arxiv-link').addEventListener('blur',loadArxiv);
$('arxiv-link').addEventListener('input',()=>{$('score').disabled=$('arxiv-link').value.trim().length>0});
new MutationObserver(()=>{if(arxivPaperText&&$('score').disabled){$('score').disabled=false;$('score').click()}}).observe($('arxiv-status'),{childList:true,characterData:true,subtree:true});
const youtubeId=value=>{try{const url=new URL(value.trim()),host=url.hostname.replace('www.','');if(host==='youtu.be')return url.pathname.slice(1).match(/^[\w-]{11}$/)?.[0]||'';if(host.endsWith('youtube.com'))return (url.searchParams.get('v')||url.pathname.split('/').pop()).match(/^[\w-]{11}$/)?.[0]||''}catch{}return''};
async function loadYouTube(){const id=youtubeId($('youtube-link').value);youtubeSnapshot=null;youtubeTranscript='';if(!$('youtube-link').value.trim()){$('youtube-status').textContent='';return}if(!id){$('youtube-status').textContent='Enter a valid YouTube video link.';return}$('youtube-status').textContent='Reading the public English transcript…';try{let response=await fetch(`https://www.youtube.com/api/timedtext?lang=en&v=${encodeURIComponent(id)}`);if(!response.ok||!(await response.clone().text()).trim())response=await fetch(`https://www.youtube.com/api/timedtext?lang=en&kind=asr&v=${encodeURIComponent(id)}`);if(!response.ok)throw new Error(`captions returned ${response.status}`);const xml=new DOMParser().parseFromString(await response.text(),'application/xml');if(xml.querySelector('parsererror'))throw new Error('captions were unreadable');youtubeTranscript=[...xml.querySelectorAll('text')].map(node=>node.textContent.replace(/\s+/g,' ').trim()).filter(Boolean).join(' ');if(youtubeTranscript.length<200)throw new Error('no public English transcript is available');let metadata={};try{metadata=await fetch(`https://www.youtube.com/oembed?url=${encodeURIComponent(`https://www.youtube.com/watch?v=${id}`)}&format=json`).then(r=>r.ok?r.json():{})}catch{}youtubeSnapshot={id,url:`https://www.youtube.com/watch?v=${id}`,title:metadata.title||`YouTube video ${id}`,channelName:metadata.author_name||'',channelUrl:metadata.author_url||'',transcriptLength:youtubeTranscript.length};if(!$('title').value.trim())$('title').value=youtubeSnapshot.title;$('youtube-status').textContent=`Read ${youtubeTranscript.length.toLocaleString()} characters from the public English transcript. The score will use this transcript.`}catch(error){$('youtube-status').textContent=`Could not read a public English transcript: ${error.message}. This linked video cannot be scored without one.`}}
$('youtube-link').addEventListener('change',loadYouTube);$('youtube-link').addEventListener('blur',loadYouTube);
const count=(t,words)=>words.reduce((n,w)=>n+(t.match(new RegExp('\\b'+w+'\\b','g'))||[]).length,0),clamp=n=>Math.max(0,Math.min(100,Math.round(n)));
$('score').addEventListener('click',()=>{const hasArxivLink=$('arxiv-link').value.trim().length>0,hasYouTubeLink=$('youtube-link').value.trim().length>0;if(hasArxivLink&&hasYouTubeLink){alert('Use either an arXiv link or a YouTube link for one submission.');return}if(hasArxivLink&&!arxivPaperText){alert('This arXiv-linked source has not loaded its complete paper text, so it cannot be scored from an abstract.');return}if(hasYouTubeLink&&!youtubeTranscript){alert('This YouTube-linked source has not loaded a public English transcript, so it cannot be scored.');return}const raw=hasArxivLink?arxivPaperText:hasYouTubeLink?youtubeTranscript:$('text').value.trim(),text=raw.toLowerCase();if(raw.length<80){alert('Please provide at least a short paragraph so there is enough text to compare.');return}const hopeful=count(text,['benefit','improve','opportunity','help','assist','promise','enable','advance','positive','potential']),anxious=count(text,['risk','harm','concern','danger','threat','limit','failure','bias','cheat','decline']),empirical=count(text,['study','data','experiment','survey','sample','result','evidence','trial','measured','method']),cautious=count(text,['might','could','may','future','perhaps','believe','predict','likely']),citations=(raw.match(/https?:\/\/|\[[^\]]+\]\([^)]*\)|\([A-Z][A-Za-z-]+,?\s*20\d\d\)/g)||[]).length,outlook=clamp(50+(hopeful-anxious)*5),evidence=clamp(35+empirical*6+citations*3-cautious*2),reliability=clamp(45+Math.min(20,raw.length/350)+Math.min(18,citations*3)+Math.min(15,empirical*2));scored={title:$('title').value.trim()||'Untitled source',outlook,evidence,reliability,excerpt:raw.slice(0,500),arxiv:arxivSnapshot,youtube:youtubeSnapshot,scoredTextLength:raw.length};$('outlook').textContent=outlook;$('evidence').textContent=evidence;$('reliability').textContent=reliability;$('comparison').textContent='Closest current map entries: '+references.map(([name,o,e,r])=>({name,d:Math.hypot(o-outlook,e-evidence,r-reliability)})).sort((a,b)=>a.d-b.d).slice(0,3).map(x=>x.name).join(', ')+'.';$('explanation').textContent=`Scored ${raw.length.toLocaleString()} characters${hasArxivLink?' from the complete arXiv HTML paper':hasYouTubeLink?' from the public YouTube transcript':''}. Cue counts — hopeful: ${hopeful}; cautionary: ${anxious}; empirical: ${empirical}; citations/links: ${citations}.`;$('prepare').disabled=false;$('result').classList.remove('mathchat-hidden');$('result').scrollIntoView({behavior:'smooth'})});
$('prepare').addEventListener('click',()=>{if(!scored)return;const submitterName=$('submitter-name').value.trim();if(!submitterName){$('submitter-name').focus();alert('Please enter your name before preparing a submission.');return}scored.arxiv=arxivSnapshot;scored.youtube=youtubeSnapshot;const entry={...scored,submitterName,submittedAt:new Date().toISOString(),submissionConfirmed:true},entries=JSON.parse(localStorage.getItem('mathchat-appendix-submissions')||'[]');entries.push(entry);localStorage.setItem('mathchat-appendix-submissions',JSON.stringify(entries));const sourceDetails=entry.arxiv?`\n\nSubmitter: ${entry.submitterName}\narXiv: ${entry.arxiv.url}\nAuthors: ${entry.arxiv.authors.map(a=>a.name+(a.affiliation?` (${a.affiliation})`:'')).join('; ')}\nCategory: ${entry.arxiv.primaryCategory||'not listed'}\nScore input: complete arXiv HTML paper (${entry.scoredTextLength.toLocaleString()} characters; abstract was not scored)\nPosition summary (arXiv abstract):\n${entry.arxiv.summary}`:entry.youtube?`\n\nSubmitter: ${entry.submitterName}\nYouTube: ${entry.youtube.url}\nChannel: ${entry.youtube.channelName||'not listed'}\nScore input: public English transcript (${entry.scoredTextLength.toLocaleString()} characters; title and description were not scored)`:`\n\nSubmitter: ${entry.submitterName}`;const issue='https://github.com/algeboy/MathChat/issues/new?title='+encodeURIComponent('Appendix submission: '+entry.title)+'&body='+encodeURIComponent(`I request review and possible public inclusion. I confirm this submission is tidy, polite, and apolitical.\n\nTitle: ${entry.title}\nScores: outlook ${entry.outlook}, evidence ${entry.evidence}, reliability ${entry.reliability}${sourceDetails}\n\nExcerpt:\n${entry.excerpt}`);$('submission-note').innerHTML=`A private preview is now available in <a href="/MathChat/appendix/">the appendix</a>. To request public review, <a href="${issue}" target="_blank" rel="noopener">open a prepared GitHub issue</a>.`});
</script>
